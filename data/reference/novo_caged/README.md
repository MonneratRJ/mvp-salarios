# NOVO CAGED Reference Data

This folder stores reference tables used to decode categorical fields from NOVO CAGED movement files.

## Structure

- `lookup_tables/`: CSV files that map code-like values to readable text labels.

## Usage

These files are not the raw CAGED movement data itself. They should be joined or used as translation tables when preparing the `CAGEDMOV` datasets for analysis.
